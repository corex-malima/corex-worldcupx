# Carga de fixture

Usar:

- `csv_templates/teams_template.csv`
- `csv_templates/matches_template.csv`

## Importante

El seed demo no es fixture oficial. Reemplazarlo por datos oficiales cuando estén validados por TTHH/Admin.

## Slots futuros

Para eliminatorias, `home_slot` y `away_slot` permiten valores como:

- `1A`
- `2B`
- `3C/D/E`

Esto permite crear el calendario antes de conocer clasificados.
