# Pendientes técnicos para Codex

## Frontend

- Conectar `useTickets` a una vista real `v_my_tickets` si se desea conservar ese contrato.
- Reemplazar datos mock por consultas reales de `teams`, `matches`, `prediction_match_scores`.
- Implementar edición completa de eliminatorias con penales.
- Conectar `BracketBoard` con `predicted_bracket_slots`.
- Agregar notificaciones persistentes.
- Agregar exportación CSV real desde vistas Supabase.

## Backend

- Sustituir seed demo por fixture oficial.
- Completar matriz oficial de dieciseisavos para 48 equipos y mejores terceros.
- Ajustar desempates FIFA completos si el negocio lo exige.
- Afinar scoring de avances por ronda con fixtures reales.
- Agregar pruebas SQL para venta concurrente y reclamo inválido.
